import React from "react";
import { Paper } from "@mui/material";

function Settings() {
  return (
    <Paper sx={{ p: 3 }}>
      <h2>Settings</h2>
      <p>Notification preferences, profile info, theme settings can be configured here.</p>
    </Paper>
  );
}

export default Settings;
